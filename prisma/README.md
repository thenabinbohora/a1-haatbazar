# Prisma

Stage 2 adds Prisma schema design and seed data only. No migration has been run against a real Supabase database yet.

Files:

- `schema.prisma`: PostgreSQL schema for the grocery ecommerce domain.
- `seed.ts`: Development seed script for categories, brands, products, variants, images, coupons, and banners.

Use placeholder or local development connection strings only until real Supabase credentials are configured outside source control.

